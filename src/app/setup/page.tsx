import { SetupForm } from "@/components/setup/SetupForm";

export default function SetupPage() {
  return <main className="setup-page"><div className="page-heading"><span className="eyebrow">보호자 설정</span><h1>아이에게 맞는 모험을 준비해요</h1><p>기기에 먼저 안전하게 저장하고, 보호자 계정 연결 시 다른 기기와 진행 기록을 이어갑니다.</p></div><SetupForm /></main>;
}
