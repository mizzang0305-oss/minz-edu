import { SetupForm } from "@/components/setup/SetupForm";

export default function SetupPage() {
  return <main className="setup-page"><div className="page-heading"><span className="eyebrow">보호자 설정</span><h1>아이에게 맞는 모험을 준비해요</h1><p>설정은 이 브라우저에만 저장되며 언제든 바꿀 수 있습니다.</p></div><SetupForm /></main>;
}
